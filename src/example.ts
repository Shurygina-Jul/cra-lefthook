/**
 * Пример для создания доки.
 *
 * @remarks
 * Это ссылка {@link core-library#Statistics | Statistics subsystem}.
 *
 * @param x - первый параметр
 * @param y - второй параметр
 * @returns возврат функции
 *
 * @beta
 */
function getExample(x: number, y: number): number {
  return (x + y) / 2.0;
}

// The TypeScript function is included for illustrative purposes.
// It is not processed by the TSDoc parser.
