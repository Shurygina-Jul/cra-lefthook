pre-commit:
  scripts:
    "eslint.js":
      runner: node
      files: "**/*.{js,jsx,ts,tsx}"
      glob: true
      include: "**/*.{js,jsx,ts,tsx}"
      exclude: "node_modules/**/*"

commit-msg:
  scripts:
    "template_checker.js":
      runner: node
